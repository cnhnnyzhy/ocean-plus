spring:
  cloud:
    nacos:
      # 服务发现
      discovery:
        server-addr:
        namespace:
        enabled: true
        group: LOCAL_GROUP
      # 配置中心
      config:
        server-addr:
        file-extension: yaml
        namespace:
        group: LOCAL_GROUP
        shared-configs[0]: